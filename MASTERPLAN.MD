# MASTERPLAN

## 1. Proje Vizyonu ve Amacı
Bu projenin amacı, Mackolik üzerinden geçmiş maç verilerini ve bahis oranlarını toplayarak **"Benzer oranlı maçların geçmişte nasıl sonuçlandığını"** anlaşılır şekilde sunan bir analiz platformu oluşturmaktır.

- **Temel Metot:** Oran-temelli benzerlik (exact / toleranslı eşleşme).
- **Hedef:** Oran dağılımlarını ve olası sonuç eğilimlerini göstermek.
- **Kapsam:** Majör ligler + aktif sezonlar + LiveData fikstürleri.
- **ML Hazırlığı:** Maç istatistikleri (şut, korner, possession) ve H2H verileri toplanarak gelecekte ML modeli eğitimi için veri tabanı oluşturulur.
- **ML Pipeline:** 5 bahis marketi (MS, KG, AU 2.5, TG, İY/MS) icin LightGBM modelleri ile "tutan bet" odakli tahminler. Precision@Confidence birincil metrik, baseline karsilastirma ile model katma degeri olcumu.

---

## 2. Sistem Bileşenleri
- **Veritabanı:** Supabase (PostgreSQL).
- **Scraper CLI (Python):** `main.py` üzerinden tüm operasyonlar.
- **Scraping:** Playwright (browser) + Scrapling (Adaptor parse + Fetcher HTTP). Wrapper: `scraping_client.py`.
- **Queue & Worker:** `match_queue` tablosu + `batch_processor.py`.
- **Fikstür Takip:** `livedata_update_fixtures.py` + `monitoring_worker.py`.
- **H2H & Stats:** `h2h_scraper.py` + `scraper_engine.py:parse_match_stats()`.
- **Backfill:** `backfill.py` — geçmiş maçlar için stats + H2H doldurma.
- **Web UI:** Next.js (`web-ui/`) + Supabase istemcisi.

---

## 3. Mimari Akışlar (Workflow)

### 3.1 Geçmiş Veri Toplama
1) `fill-queue` aktif sezonlardan maç linklerini kuyruğa ekler.
2) `run-worker` Playwright ile maçı işler ve `matches` tablosuna upsert yapar.
3) Aynı anda `match_stats` ve `match_h2h` tablolarına istatistik/H2H verileri yazılır.
4) `match_queue.status` `SUCCESS / BAD_DATA / ERROR` olarak güncellenir.

### 3.2 Fikstür Senkronizasyonu + Monitoring
1) `update-fixtures` LiveData üzerinden gelecek maçları kuyruğa `MONITORING` olarak yazar.
2) `run-monitoring-worker` ±24 saat penceresinde maçları kontrol eder.
3) Skor geldiyse `SUCCESS`, aksi halde `MONITORING` kalır.

### 3.3 Stats + H2H Veri Toplama
1) Her `process_full_match()` çağrısında, skor varsa `parse_match_stats()` çalışır → `match_stats` tablosuna upsert.
2) Her maç için `scrape_h2h()` çalışır → `match_h2h` tablosuna upsert.
3) Stats/H2H hataları ana maç verisini etkilemez (ayrı try-except).
4) `backfill` komutu ile geçmiş maçlar için toplu doldurma yapılabilir.

### 3.4 ML Tahmin Pipeline
1) `train-models` tum veriyi Supabase'den ceker (matches + match_stats + match_h2h).
2) Feature engineering: ~125 feature (oran implied prob, rolling takim istatistikleri, baglamsal).
3) Temporal split (train / val 2 ay / test 6 ay) ile LightGBM modelleri egitilir.
4) 5 market: MS (1/X/2), KG (Var/Yok), AU 2.5 (Alt/Ust), TG (0-1/2-3/4-5/6+), IY/MS (9 sinif birlesik).
5) Birincil metrik: Precision @ Confidence (model "eminim" dediginde gercekten tutma orani).
6) Baseline karsilastirma: Naive odds baseline ile lift metrigi hesaplanir (model vs bahisci).
7) Feature importance gruplama: odds-derived vs rolling stats vs contextual katki orani izlenir.
8) Modeller + threshold'lar + kalibrasyon + diagnostics `ml_models/` dizinine kaydedilir.
9) `predict` gelecek maclar icin tahmin uretir, confident picks (cok_emin/emin/olasi) tespit eder.
10) Tahminler `ml_predictions` tablosuna yazilir.

### 3.5 Analiz & Web UI
- `matches` tablosu tek kaynak gerçektir (fikstür + geçmiş).
- Oran benzerliği (toleranslı) `/match/[id]` ve `/analysis/[id]` ile sunulur.
- Tam eşleşme akışı `/perfect-match` ekranında gösterilir.

---

## 4. Veritabanı Şeması (Güncel)
**Tek kaynak tablo:** `matches`
**Not:** `weekly_fixtures` deprecated (legacy).

### A. Referans Tabloları
**1) `leagues`**
```sql
CREATE TABLE leagues (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  country VARCHAR(50),
  created_at TIMESTAMP DEFAULT NOW()
);
```

**2) `seasons`**
```sql
CREATE TABLE seasons (
  id SERIAL PRIMARY KEY,
  league_id INT REFERENCES leagues(id),
  season_year VARCHAR(20) NOT NULL,
  mackolik_id INT NOT NULL UNIQUE,
  total_weeks INT DEFAULT 38,
  is_active BOOLEAN DEFAULT TRUE
);
```

### B. Operasyon Tabloları
**3) `match_queue`**
```sql
CREATE TABLE match_queue (
  match_code VARCHAR(50) PRIMARY KEY,
  match_url TEXT,
  season_id INT,
  season_name VARCHAR(100),
  week INT,
  status VARCHAR(20) DEFAULT 'PENDING', -- PENDING, MONITORING, SUCCESS, ERROR, BAD_DATA
  error_log TEXT,
  last_try_at TIMESTAMP
);
```

**4) `matches`**
```sql
CREATE TABLE matches (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  match_code VARCHAR(50) UNIQUE,
  home_team VARCHAR(100),
  away_team VARCHAR(100),
  league VARCHAR(100),
  season VARCHAR(50),
  match_date TIMESTAMP,

  -- SKOR / DURUM
  score_ft VARCHAR(20),
  score_ht VARCHAR(20),
  status VARCHAR(20), -- MS / FIXTURE / Ert vb.

  -- MS 1/X/2
  ms_1 FLOAT, ms_x FLOAT, ms_2 FLOAT,

  -- Çifte Şans
  cs_1x FLOAT, cs_12 FLOAT, cs_x2 FLOAT,

  -- İY/MS
  iyms_11 FLOAT, iyms_1x FLOAT, iyms_12 FLOAT,
  iyms_x1 FLOAT, iyms_xx FLOAT, iyms_x2 FLOAT,
  iyms_21 FLOAT, iyms_2x FLOAT, iyms_22 FLOAT,

  -- ALT/ÜST & KG
  au_15_alt FLOAT, au_15_ust FLOAT,
  au_25_alt FLOAT, au_25_ust FLOAT,
  kg_var FLOAT, kg_yok FLOAT,

  -- GOL ARALIĞI
  tg_0_1 FLOAT, tg_2_3 FLOAT, tg_4_5 FLOAT, tg_6_plus FLOAT
);
```

### C. İstatistik Tabloları
**5) `match_stats`**
```sql
CREATE TABLE match_stats (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  match_code VARCHAR(50) UNIQUE NOT NULL REFERENCES matches(match_code),
  shots_home INT, shots_away INT,
  shots_on_home INT, shots_on_away INT,
  corners_home INT, corners_away INT,
  possession_home FLOAT, possession_away FLOAT,
  fouls_home INT, fouls_away INT,
  offsides_home INT, offsides_away INT,
  scraped_at TIMESTAMP DEFAULT NOW()
);
```

**6) `match_h2h`**
```sql
CREATE TABLE match_h2h (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  match_code VARCHAR(50) UNIQUE NOT NULL REFERENCES matches(match_code),
  form_home VARCHAR(10), form_away VARCHAR(10),
  standing_home INT, standing_away INT,
  points_home INT, points_away INT,
  tg_dist_home JSONB, tg_dist_away JSONB,
  kg_pct_home FLOAT, kg_pct_away FLOAT,
  au25_over_pct_home FLOAT, au25_over_pct_away FLOAT,
  scorers_home JSONB, scorers_away JSONB,
  referee_stats JSONB,
  h2h_total JSONB,
  scraped_at TIMESTAMP DEFAULT NOW()
);
```

### D. ML Tabloları
**7) `ml_predictions`**
```sql
CREATE TABLE ml_predictions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  match_code VARCHAR(50) NOT NULL REFERENCES matches(match_code),
  market VARCHAR(20) NOT NULL,
  probabilities JSONB NOT NULL,
  predicted_outcome VARCHAR(20),
  confidence FLOAT,
  confident_picks JSONB,
  model_version VARCHAR(50),
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(match_code, market, model_version)
);
```

**8) `ml_model_runs`**
```sql
CREATE TABLE ml_model_runs (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  model_version VARCHAR(50) UNIQUE NOT NULL,
  trained_at TIMESTAMP DEFAULT NOW(),
  train_size INT,
  test_size INT,
  metrics JSONB,
  feature_count INT,
  notes TEXT
);
```

### E. Gunluk Kupon Tablosu (DEPRECATED)
`daily_picks` ve `backtest_picks` tablolari artik kullanilmiyor. Wilson Score sistemi kaldirildi, ML confident picks sistemi aktif.

---

## 5. Statüler ve İş Kuralları
- `match_queue.status`: `PENDING`, `MONITORING`, `SUCCESS`, `ERROR`, `BAD_DATA`.
- `matches.status`: Mackolik kaynaklı durum (`MS`, `Ert`, vb.).
- `BAD_DATA`: ev/deplasman/lig/sezon eksikse veya geçmiş maçta skor yoksa.
- `MONITORING`: fikstürler ve skoru gelmemiş maçlar.
- `SUCCESS`: skorlar mevcut ve maç tarihi geçmiş.

---

## 6. Proje Durumu (Current Status)
- [x] Supabase şeması ve `matches` tek kaynak kurgusu.
- [x] CLI komutları (`fill-queue`, `update-fixtures`, `run-worker`, `run-monitoring-worker`, `backfill`).
- [x] Playwright + Scrapling tabanlı scraper (Selenium'dan geçiş tamamlandı).
- [x] LiveData fikstür senkronu ve monitoring akışı.
- [x] Queue onarım ve durum senkronu (`repair_queue.py`).
- [x] Maç istatistikleri (`match_stats`) ve H2H (`match_h2h`) veri toplama.
- [x] Backfill komutu (son 6 ay stats + H2H doldurma).
- [x] Web UI: Fikstür, oran arama, benzerlik, analiz ve perfect-match sayfaları.
- [x] Web UI: Mobile-first responsive (bottom tab bar, kart görünümü, kategori tabları, bottom sheet takvim, touch target'lar).
- [x] ~~Web UI: Gunun Kuponu sayfasi (Wilson Score tabanli)~~ → KALDIRILDI, ML confident picks aktif.
- [x] ML Pipeline: 5 market LightGBM + Precision@Confidence + baseline karsilastirma + diagnostics.

---

## 7. Next Steps
- [x] ML modeli icin feature engineering (match_stats + oranlar + rolling stats).
- [x] ML tahmin pipeline (5 market LightGBM + confident picks).
- [x] ML baseline karsilastirma ve diagnostics (lift, feature gruplama).
- [x] ML tahminlerini Web UI'da gosterme (MLPredictionsDashboard, MLBacktestDashboard).
- [ ] ML model hyperparameter tuning ve cross-validation.
- [ ] Veri kalite raporu (eksik skor/oran, eksik match_date).
- [ ] Supabase sorgularında sayfalama/caching stratejileri.
- [ ] Web UI için temel smoke-test akışı.
- [ ] Monitoring window ve status heuristiklerinin ince ayarı.
- [ ] match_stats ve match_h2h verilerini Web UI'da gösterme.

---

## 8. Changelog
**2026-03-20** — **ML Pipeline: "Tutan Bet" Yeniden Tasarim**
  - **Why:** Value bet yerine "tutan bet" odakli yaklasim. Model "eminim" dediginde gercekten tutmasi lazim. Wilson Score sistemi ROI negatif kaldigi icin kaldirildi.
  - **Scope:**
    - **Guncellenen:** `ml/train.py` (baseline karsilastirma, lift metrigi, feature importance gruplama, diagnostics.json), `ml/config.py` (IY→IYMS, TG 4 sinif, confidence thresholds), `ml/features.py` (IYMS 9 sinif target, TG 4 sinif, IYMS normalized probs), `ml/predict.py` (confident picks sistemi), `MASTERPLAN.MD`
    - **Silinen:** `backtest_daily_picks.py`, `web-ui/app/daily-picks/`, `web-ui/components/DailyPicksDashboard.tsx`, `web-ui/app/api/matches/daily-picks/route.ts`, `web-ui/app/api/matches/backtest/route.ts`
  - **Notes:**
    - 5 market: MS, KG, AU 2.5, TG (4 sinif: 0-1/2-3/4-5/6+), IY/MS (9 sinif birlesik)
    - ~125 feature: oran implied prob, rolling takim istatistikleri, baglamsal
    - Birincil metrik: Precision @ Confidence (threshold bazli)
    - Baseline testi: Naive odds baseline + lift metrigi (model katma degeri olcumu)
    - Feature importance gruplama: odds-derived vs rolling stats vs contextual
    - Diagnostics: `ml_models/diagnostics.json` (her market icin lift, feature dagılımı)
    - ILK SONUCLAR: KG lift=1.219 (iyi), MS lift=0.978 (bahisciyi kopyaliyor), AU25 lift=1.004 (notr), IYMS lift=1.015 (marjinal)

**2026-03-16** — **ML Tahmin Pipeline**
  - **Why:** Wilson+EV backtest ROI negatif kaldı. ML modeli bahisçinin eksik fiyatladığı sinyalleri yakalamak için.
  - **Scope:**
    - **Yeni:** `ml/__init__.py`, `ml/config.py`, `ml/features.py`, `ml/train.py`, `ml/predict.py`, `sql/create_ml_predictions.sql`, `ml_models/` dizini
    - **Güncellenen:** `main.py` (train-models, predict komutları), `requirements.txt` (lightgbm, scikit-learn, pandas, numpy, joblib), `MASTERPLAN.MD`
  - **Notes:**
    - 5 market: MS, KG, AU 2.5, TG, İY (LightGBM)
    - ~93 feature: oran implied prob, H2H pre-match, rolling takım istatistikleri (shift(1) leakage-free), bağlamsal
    - Temporal split (train/val/test), early stopping, ROI simülasyonu
    - Value bet: model_prob > implied_prob + %5 edge
    - DB: ml_predictions + ml_model_runs tabloları

**2026-03-16** — **Scrapling Geçişi + Ek Veri Toplama + Kod Temizliği**
  - **Why:** Selenium → Playwright + Scrapling geçişi (daha stabil, modern API). Ek veri toplama (stats + H2H) ML hazırlığı için. Kullanılmayan dosyalar temizlendi.
  - **Scope:**
    - **Silinen:** `notify_perfect_matches.py`, `generate_daily_picks.py`, `debug_week.py`, `debug_week_html.py`, `migrate_fixtures.py`
    - **Yeni:** `scraping_client.py`, `h2h_scraper.py`, `backfill.py`, `sql/create_match_stats_h2h.sql`
    - **Güncellenen:** `scraper_engine.py`, `batch_processor.py`, `monitoring_worker.py`, `main.py`, `requirements.txt`, `create_tables.py`, `RULES.MD`, `MASTERPLAN.MD`
  - **Notes:**
    - Scraping: Playwright (domcontentloaded) + Scrapling Adaptor (CSS parse) + Fetcher (HTTP)
    - DB: `match_stats` (şut, korner, possession vb.) ve `match_h2h` (form, puan durumu, KG%, AU%, hakem) tabloları
    - CLI: `backfill` komutu (son 6 ay stats + H2H toplu doldurma)
    - Temizlik: notify-perfect-matches, generate-daily-picks komutları kaldırıldı

**2026-03-14** — **Günün Kuponu (Daily Picks)**
  - **Why:** Kullanıcıya günlük otomatik kupon önerileri sunmak. Wilson Score Lower Bound ile istatistiksel güvenirlik sağlayan algoritma.
  - **Scope:** `generate_daily_picks.py`, `main.py`, `web-ui/app/daily-picks/`, `web-ui/components/DailyPicksDashboard.tsx`, `web-ui/app/api/matches/daily-picks/route.ts`, `web-ui/lib/stats/wilson.ts`, `web-ui/components/Header.tsx`
  - **Notes:** Günlük 17:00 snapshot + canlı hesaplama + backtest geçmişi. Güvenli Kupon (yüksek wilson) + Değer Kuponu (yüksek EV) ikili yaklaşım.

**2026-03-11** — **Mobile-First Responsive Overhaul**
  - **Why:** Tüm ekranların mobilde (375-414px) rahat kullanılmasını sağlamak. Bahis platformlarında trafik ağırlıklı mobilden gelir.
  - **Scope:** `web-ui/components/Header.tsx`, `web-ui/components/FixtureTable.tsx`, `web-ui/components/MatchOddsTable.tsx`, `web-ui/components/OddsSearchDashboard.tsx`, `web-ui/components/FixtureDatePicker.tsx`, `web-ui/components/AnalysisDashboard.tsx`, `web-ui/components/PerfectMatchDashboard.tsx`, `web-ui/components/SearchableSelect.tsx`, `web-ui/app/globals.css`, 5× page layout dosyaları, `web-ui/DESIGN_GUIDE.md`
  - **Notes:**
    - Header: Mobilde bottom tab bar (3 ikon+label), masaüstünde üst nav korundu
    - FixtureTable: Mobilde kart görünümü (tablo yerine), filtre butonları büyütüldü
    - MatchOddsTable: Mobilde kategori tab sistemi (ODDS_CATEGORIES bazlı), sticky header/column düzeltmeleri
    - OddsSearchDashboard: Açılır/kapanır sidebar, mobil tab-based tablo
    - FixtureDatePicker: 3 gün strip + bottom sheet takvim
    - Tüm bileşenler: 44px+ touch target'lar, responsive font boyutları
    - DESIGN_GUIDE.md: Detaylı mobil-first responsive kuralları eklendi

**2026-01-20** — **LiveData Fikstür Senkronu & Monitoring Worker**
  - **Why:** Günlük fikstürleri kuyruğa almak ve skorları otomatik takip etmek.
  - **Scope:** `livedata_update_fixtures.py`, `monitoring_worker.py`, `queue_manager.py`
  - **Notes:** MONITORING statüsü ve ±24 saat takip penceresi.

**2026-01-20** — **Queue Onarım Senkronu**
  - **Why:** `matches` ile `match_queue` statülerini eşitlemek.
  - **Scope:** `repair_queue.py`
  - **Notes:** Tarih/score bazlı status normalizasyonu.

**2026-01-20** — **Web UI: Mükemmel Eşleşme Sayfası** (by Antigravity)
  - **Why:** Bugünkü maçları geçmiş maçlarla karşılaştırarak tam oran eşleşmesi bulan sayfa.
  - **Scope:** `web-ui/app/perfect-match`, `web-ui/components/PerfectMatchDashboard.tsx`, `web-ui/components/Header.tsx`
  - **Notes:** Kategori bazlı filtreleme ve eşleşme listesi.

**2026-01-14** — **Web UI: Sürpriz Ekranları Kaldırıldı** (by Antigravity)
  - **Why:** Ürün odağını temel analiz akışlarına çekmek.
  - **Scope:** `web-ui/app/surprise`, `web-ui/app/surprise-draw`, `web-ui/components/SurpriseDashboard.tsx`, `web-ui/components/SurpriseTable.tsx`, `web-ui/components/SurpriseDrawDashboard.tsx`, `web-ui/components/SurpriseDrawTable.tsx`, `web-ui/components/Header.tsx`
  - **Notes:** Sürpriz sayfaları kaldırıldı ve header navigasyonundan çıkarıldı.

**2026-01-14** — **Web UI: Oran Arama ve Analiz Genişletmeleri** (by Antigravity)
  - **Why:** Çoklu oran araması ve sonuç dağılımları.
  - **Scope:** `web-ui/app/odds-search`, `web-ui/components/OddsSearchDashboard.tsx`
  - **Notes:** Filtreli oran arama ve sonuç dağılımı panelleri eklendi.

**2026-01-14** — **Web UI: Benzerlik Analizi Sayfaları** (by Antigravity)
  - **Why:** Tek maç üzerinden benzer oranlı geçmiş maçları ve analiz istatistiklerini sunmak.
  - **Scope:** `web-ui/app/match/[id]`, `web-ui/app/analysis/[id]`, `web-ui/components/AnalysisDashboard.tsx`
  - **Notes:** Kategori eşleşmesi ve tolerans filtreleri eklendi.

**2026-01-12** — **Veri Yapısı Birleştirme** (by Antigravity)
  - **Why:** Fikstür ve arşiv verilerini tek tabloda tutmak.
  - **Scope:** `scraper_engine.py`, `migrate_fixtures.py`
  - **Notes:** `weekly_fixtures` tablosu devre dışı bırakıldı.
